/** Shared card dimensions so the windowed grid math matches the rendered cards. */
export const CARD_W = 160; // px
export const CARD_GAP = 12; // px (Tailwind gap-3)
export const CARD_H = CARD_W * 1.5; // 2:3 poster → 240px
export const ROW_H = CARD_H + CARD_GAP;
export const OVERSCAN_ROWS = 3;
