/** Marks a profile as restricted by parental controls. */
export default function KidsChip() {
  return (
    <span className="shrink-0 rounded bg-white/10 px-1.5 py-0.5 text-[10px] font-medium uppercase tracking-wide text-muted">
      Kids
    </span>
  );
}
